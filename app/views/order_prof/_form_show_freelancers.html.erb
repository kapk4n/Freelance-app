<%= turbo_frame_tag :freela_list, target: :_top do %>
<div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true" data-controller="bs-modal">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel"> Freelancers </h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
  <%= form_with url: ord_prof_take_fr_path, method: :post do |form| %>
    <div class="modal-body">
      <% @preorder.each do |i| %>
        <%= form.label @freelancers.find_by(id: i.freelancer_id).id %>
        <%= button_to "root_path", ord_prof_take_fr_path(id: params[:id], fr_id: @freelancers.find_by(id: i.freelancer_id).id) %>
        <br>
      <% end %>



      <div class="modal-footer">

      <%#= form.submit "Submit", class: "btn btn-success", data: {action: "click->bs-modal#submitEnd" }, id: 'reg-btn' %>
      </div>
<% end %>
    </div>
  </div>
  </div>
</div>
<% end %>